{% extends 'base.html.twig' %}

{% block title %}Quizz{% endblock %}

{% block body %}
    <div class="container">
        <h1>Catégories</h1>
        {% for categorie in categories %}
            <div>
                <p>{{ categorie.name }}</p>

            </div>
        {% endfor %}
    </div>
{% endblock %}
